version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npx playwright install
      - npm test
artifacts:
  files:
    - "**/*"
